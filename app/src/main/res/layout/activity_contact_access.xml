<!-- res/layout/activity_contact_access.xml -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="#FFFFFF"
    tools:ignore="ExtraText">

    <!-- Banner Image -->
    <ImageView
        android:id="@+id/banner_image"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:src="@drawable/img"
        android:scaleType="centerCrop"
        android:layout_alignParentTop="true" />

    <!-- Welcome Text -->
    <TextView
        android:id="@+id/welcome_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Welcome to Your Contacts"
        android:textSize="24sp"
        android:textColor="#000000"
        android:textStyle="bold"
        android:layout_below="@id/banner_image"
        android:layout_marginTop="24dp"
        android:layout_centerHorizontal="true" />

    <!-- Subtitle Text -->
    <TextView
        android:id="@+id/subtitle_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Access and manage all your contacts here"
        android:textSize="16sp"
        android:textColor="#888888"
        android:layout_below="@id/welcome_text"
        android:layout_marginTop="8dp"
        android:layout_centerHorizontal="true" />

    <!-- Access Contacts Button -->
    <Button
        android:id="@+id/access_contacts_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="#4CAF50"
        android:text="Access Contacts"
        android:textColor="#FFFFFF"
        android:textSize="18sp"
        android:layout_below="@id/subtitle_text" /><!-- Placed below the subtitle -->

</RelativeLayout>
